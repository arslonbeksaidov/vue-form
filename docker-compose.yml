services:
  app:
    image: node:18-alpine
    command: sh -c "npm install && npm run serve"
    expose:
      - 3000
    container_name: vue-forum
    ports:
      - 3000:3000
    working_dir: /app
    volumes:
      - ./:/app
