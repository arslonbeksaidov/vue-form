services:
  app:
    image: node:18-alpine
    command: sh -c "npm install && npm run serve"
    container_name: vue-forum
    ports:
      - 3000:3000
    working_dir: /app
    volumes:
      - ./:/app
